node_modules/
.DS_Store
*.log
.env

# Build output. Generated by `npm run build`, shipped from the npm tarball via
# the "files" field, and rebuilt by `prepare` for installs straight from git.
# It used to be committed, which is how 0.2.0 managed to publish a dist/ whose
# hand-written .d.ts had drifted from src/ without anyone noticing.
dist/
